root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "./tmp/main"
  delay = 200
  stop_on_error = true
  include_ext = ["go", "sql", "env"]
  exclude_dir = ["tmp", "vendor", "node_modules", "db/query"]

[log]
  time = true

[misc]
  clean_on_exit = true
